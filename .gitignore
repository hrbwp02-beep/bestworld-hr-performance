# OS / editor
Thumbs.db
.DS_Store
*.log

# dependencies (none currently — CDN based)
node_modules/

# local env / secrets (do NOT commit service-role keys)
.env
.env.local
*.local.env

# build output
dist/
build/

# ไฟล์ที่มีรหัสผ่าน/บันทึกภายใน (ห้าม commit)
*.xlsx
*-NOTES.md

.vercel
